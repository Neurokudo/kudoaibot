#!/bin/bash
# Алиас для быстрой проверки обработчиков

echo "🔍 Быстрая проверка обработчиков..."

# Переходим в корень проекта
cd "$(dirname "$0")"

# Запускаем Python скрипт
python3 scripts/check_handlers.py
